name: Frequent News Sync

on:
  schedule:
    - cron: '22,44 * * * *'
  workflow_dispatch:

# --- ADD THIS BLOCK RIGHT HERE ---
concurrency:
  group: "pages"
  cancel-in-progress: true # This kills the old stuck build if a new one starts
# ---------------------------------

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write      # Added for Pages permission
      id-token: write   # Added for Pages permission
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      # ... rest of your steps (Python, Scraper, Git Push) ...
